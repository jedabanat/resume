% Geometry package for setting page layout, specifying letter paper size with 0.5-inch margins.
\usepackage[a4paper, left=1cm, top=.5in, right=1cm, bottom=0.5in, footskip=.5cm]{geometry}

% mdwlist package for compact lists with tighter spacing.
\usepackage{mdwlist}

% Disables hyphenation throughout the document.
\usepackage[none]{hyphenat}

% Uses TeX Gyre Pagella font
%\usepackage{tgpagella}

% Hyperref package for creating hyperlinks and hidelinks option to hide link formatting.
\usepackage[hidelinks]{hyperref}

% Ragged2e package for improved text justification.
\usepackage[document]{ragged2e}